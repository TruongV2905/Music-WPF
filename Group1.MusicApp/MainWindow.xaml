<Window x:Class="Group1.MusicApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
        xmlns:views="clr-namespace:Group1.MusicApp.Views"
        mc:Ignorable="d"
        Title="🎧 Music Player"
        Height="744"
        Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#0B0B0B"
        Loaded="Window_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/> <!-- Sidebar -->
            <ColumnDefinition Width="*"/>   <!-- Main View -->
        </Grid.ColumnDefinitions>

        <!-- 🌙 Sidebar -->
        <DockPanel Grid.Column="0">
            <DockPanel.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#1E1E1E" Offset="0" />
                    <GradientStop Color="#121212" Offset="1" />
                </LinearGradientBrush>
            </DockPanel.Background>

            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="15,25,0,30">
                    <TextBlock Text="🎵"
                               FontSize="28"
                               Foreground="#1DB954"
                               Margin="0,0,10,0"/>
                    <TextBlock Text="My Playlist"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#1DB954"/>
                </StackPanel>

                <ListBox x:Name="lstPlaylist"
                         Background="Transparent"
                         Foreground="#F5F5F5"
                         BorderThickness="0"
                         FontSize="16"
                         Margin="15,0,0,0"
                         Cursor="Hand">
                    <ListBoxItem Content="🔍  Tìm kiếm" Margin="0,5" Selected="SearchMenuItem_Selected"/>
                    <ListBoxItem Content="❤️  Playlist của tôi" Margin="0,5" Selected="PlaylistMenuItem_Selected"/>
                    <ListBoxItem Content="🕒  Recently Played" Margin="0,5"/>
                    <ListBoxItem Content="🤖  AI Mood Mix" Margin="0,5"/>
                </ListBox>
            </StackPanel>
        </DockPanel>

        <!-- 🌟 Main View -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Search -->
                <RowDefinition Height="*"/>    <!-- List -->
                <RowDefinition Height="Auto"/> <!-- Now Playing -->
                <RowDefinition Height="Auto"/> <!-- Lyrics -->
            </Grid.RowDefinitions>

            <!-- 🔍 Search bar - Giữ nguyên vị trí ban đầu -->
            <DockPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBox x:Name="txtSearch"
                         Width="500"
                         Height="45"
                         FontSize="16"
                         materialDesign:HintAssist.Hint="Tìm bài hát hoặc ca sĩ..."
                         Style="{DynamicResource MaterialDesignFilledTextBox}"
                         Margin="0,0,15,0"
                         Foreground="#FFF"
                         Background="#181818"
                         BorderBrush="#333"
                         KeyDown="txtSearch_KeyDown" />
                <Button x:Name="btnSearch"
                        Content="Tìm kiếm"
                        Width="100"
                        Background="#1DB954"
                        Foreground="White"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Style="{DynamicResource MaterialDesignRaisedButton}"
                        Click="btnSearch_Click">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="15" Color="#1DB954" ShadowDepth="0" />
                    </Button.Effect>
                </Button>
            </DockPanel>

            <!-- 🎵 Danh sách bài hát - Giữ nguyên vị trí ban đầu, chỉ thêm nút playlist -->
            <Border x:Name="SearchResultsContainer" Grid.Row="1" Background="#121212" CornerRadius="10" Padding="10">
                <ListView x:Name="lstTracks"
                          Foreground="White"
                          Background="#181818"
                          BorderThickness="0"
                          SelectionChanged="lstTracks_SelectionChanged"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="5">
                                    <Image Width="60" Height="60"
                                           Margin="0,0,10,0"
                                           Stretch="UniformToFill"
                                           VerticalAlignment="Center"
                                           ClipToBounds="True">
                                        <Image.Source>
                                            <Binding Path="AlbumImageUrl"/>
                                        </Image.Source>
                                    </Image>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" />
                                        <TextBlock Text="{Binding ArtistName}" FontSize="12" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                                
                                <!-- Nút thêm vào playlist - Module độc lập, không ảnh hưởng chức năng khác -->
                                <Button Grid.Column="2"
                                        Content="➕"
                                        Width="40" Height="40"
                                        Background="Transparent"
                                        Foreground="#1DB954"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="Thêm vào playlist"
                                        Click="AddToPlaylistButton_Click"
                                        Tag="{Binding}"
                                        Margin="10,0,0,0"
                                        FontSize="20"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <!-- Playlist View - Module độc lập, ẩn mặc định -->
            <views:PlaylistView x:Name="PlaylistViewControl"
                                Grid.Row="1"
                                Visibility="Collapsed"
                                TrackPlayRequested="PlaylistView_TrackPlayRequested"
                                CloseRequested="PlaylistView_CloseRequested"/>

            <!-- 🎧 Media Player ẩn -->
            <MediaElement x:Name="mediaPlayer"
                          LoadedBehavior="Manual"
                          UnloadedBehavior="Stop"
                          Volume="0.8"
                          Visibility="Collapsed"/>

            <!-- ▶ Now Playing -->
            <Border Grid.Row="2"
                    Background="#181818"
                    CornerRadius="15"
                    Padding="20"
                    Margin="0,15,0,0">
                <StackPanel>
                    <!-- Player Controls -->
                    <DockPanel Margin="0,0,0,15">
                        <Image x:Name="imgCover"
                               Width="90" Height="90"
                               Stretch="UniformToFill"
                               DockPanel.Dock="Left"
                               Margin="0,0,15,0"/>
                        <StackPanel DockPanel.Dock="Left" VerticalAlignment="Center">
                            <TextBlock x:Name="lblNowPlaying"
                                       Text="Chưa chọn bài"
                                       Foreground="White"
                                       FontSize="20"
                                       FontWeight="Bold"/>
                            <TextBlock x:Name="lblArtist"
                                       Text=""
                                       Foreground="#A0A0A0"
                                       FontSize="14"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="⏮️" Width="50" Height="50" Margin="10,0" Background="#222" Foreground="White" BorderBrush="Transparent" Cursor="Hand"/>
                            <Button Content="⏯️" Width="50" Height="50" Margin="10,0" Background="#1DB954" Foreground="White" BorderBrush="Transparent" Cursor="Hand" Click="Button_Click"/>
                            <Button Content="⏭️" Width="50" Height="50" Margin="10,0" Background="#222" Foreground="White" BorderBrush="Transparent" Cursor="Hand"/>
                            <Button Content="💖" Width="50" Height="50" Margin="10,0" Background="#222" Foreground="#1DB954" BorderBrush="Transparent" Cursor="Hand"/>
                        </StackPanel>
                    </DockPanel>

                    <!-- Metadata Section -->
                    <Border x:Name="metadataPanel"
                            Background="#1E1E1E"
                            CornerRadius="10"
                            Padding="15"
                            Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Duration -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Duration"
                                           Foreground="#A0A0A0"
                                           FontSize="12"
                                           Margin="0,0,0,5"/>
                                <TextBlock x:Name="lblDuration"
                                           Text="--:--"
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="SemiBold"/>
                            </StackPanel>

                            <!-- Popularity -->
                            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                <TextBlock Text="Popularity"
                                           Foreground="#A0A0A0"
                                           FontSize="12"
                                           Margin="0,0,0,5"/>
                                <TextBlock x:Name="lblPopularity"
                                           Text="--%"
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="SemiBold"/>
                            </StackPanel>

                            <!-- Release Date -->
                            <StackPanel Grid.Column="2" Margin="0,0,10,0">
                                <TextBlock Text="Release Date"
                                           Foreground="#A0A0A0"
                                           FontSize="12"
                                           Margin="0,0,0,5"/>
                                <TextBlock x:Name="lblReleaseDate"
                                           Text="Unknown"
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="SemiBold"/>
                            </StackPanel>

                            <!-- Album -->
                            <StackPanel Grid.Column="3">
                                <TextBlock Text="Album"
                                           Foreground="#A0A0A0"
                                           FontSize="12"
                                           Margin="0,0,0,5"/>
                                <TextBlock x:Name="lblAlbum"
                                           Text="Unknown"
                                           Foreground="White"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Audio Features Section -->
                    <Border x:Name="audioFeaturesPanel"
                            Background="#1E1E1E"
                            CornerRadius="10"
                            Padding="15"
                            Margin="0,10,0,0"
                            Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="Audio Features"
                                       Foreground="White"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Energy -->
                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="Energy"
                                               Foreground="#A0A0A0"
                                               FontSize="11"
                                               Margin="0,0,0,3"/>
                                    <TextBlock x:Name="lblEnergy"
                                               Text="--"
                                               Foreground="#1DB954"
                                               FontSize="14"
                                               FontWeight="SemiBold"/>
                                </StackPanel>

                                <!-- Danceability -->
                                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                    <TextBlock Text="Danceability"
                                               Foreground="#A0A0A0"
                                               FontSize="11"
                                               Margin="0,0,0,3"/>
                                    <TextBlock x:Name="lblDanceability"
                                               Text="--"
                                               Foreground="#1DB954"
                                               FontSize="14"
                                               FontWeight="SemiBold"/>
                                </StackPanel>

                                <!-- Valence -->
                                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                                    <TextBlock Text="Valence"
                                               Foreground="#A0A0A0"
                                               FontSize="11"
                                               Margin="0,0,0,3"/>
                                    <TextBlock x:Name="lblValence"
                                               Text="--"
                                               Foreground="#1DB954"
                                               FontSize="14"
                                               FontWeight="SemiBold"/>
                                </StackPanel>

                                <!-- Acousticness -->
                                <StackPanel Grid.Column="3">
                                    <TextBlock Text="Acousticness"
                                               Foreground="#A0A0A0"
                                               FontSize="11"
                                               Margin="0,0,0,3"/>
                                    <TextBlock x:Name="lblAcousticness"
                                               Text="--"
                                               Foreground="#1DB954"
                                               FontSize="14"
                                               FontWeight="SemiBold"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- 🎤 Lyrics - Giữ nguyên vị trí ban đầu -->
            <Border Grid.Row="3"
                    Background="#101010"
                    CornerRadius="10"
                    Padding="15"
                    Margin="0,15,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="txtLyrics"
                               Foreground="White"
                               FontSize="14"
                               TextWrapping="Wrap"
                               LineHeight="24"
                               Text="🎶 Chưa có lời bài hát..." />
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Window>
